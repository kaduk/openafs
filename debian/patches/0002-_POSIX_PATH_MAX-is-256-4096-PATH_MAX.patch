From: Benjamin Kaduk <kaduk@mit.edu>
Date: Sat, 6 Sep 2014 00:28:04 -0400
Subject: _POSIX_PATH_MAX is 256 << 4096 == PATH_MAX

---
 src/util/dirpath.hin | 6 +++++-
 1 file changed, 5 insertions(+), 1 deletion(-)

diff --git a/src/util/dirpath.hin b/src/util/dirpath.hin
index 9ffd341..127d8ed 100644
--- a/src/util/dirpath.hin
+++ b/src/util/dirpath.hin
@@ -75,7 +75,11 @@
 #ifdef AFS_NT40_ENV
 #define AFSDIR_PATH_MAX MAX_PATH
 #else /* unices */
-#define AFSDIR_PATH_MAX    _POSIX_PATH_MAX
+# if defined(PATH_MAX)
+#  define AFSDIR_PATH_MAX  PATH_MAX
+# else
+#  define AFSDIR_PATH_MAX    _POSIX_PATH_MAX
+# endif
 #endif
  
 
